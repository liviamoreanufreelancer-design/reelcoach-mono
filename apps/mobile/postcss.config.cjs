// postcss.config.cjs — picked up automatically by Vite for CSS processing.
// Using @tailwindcss/postcss (the Tailwind 4 PostCSS plugin) instead of
// @tailwindcss/vite to avoid the CachedInputFileSystem incompatibility.
module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
